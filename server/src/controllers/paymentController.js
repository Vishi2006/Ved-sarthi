const createPaymentIntent = async (_req, res) => {
  // Placeholder: wire Razorpay/Stripe later.
  res.json({ message: "Payments not implemented yet" });
};

module.exports = { createPaymentIntent };
